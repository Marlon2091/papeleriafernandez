import PlaceholderPage from '../components/PlaceholderPage';

export default function UsuariosPage() {
  return <PlaceholderPage title="Usuarios" description="Gestión de usuarios y roles (solo admin), pendiente de construir." />;
}
